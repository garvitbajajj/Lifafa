{
  "name": "lifafa",
  "private": true,
  "type": "module",
  "description": "Offline UPI payments carried over a mesh of nearby phones and settled exactly once.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "check": "vitest run"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  }
}
